<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

   <Button
        android:id="@+id/btn_camera"
        android:layout_width="230dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:text="@string/btn_main_photo" />

    <Button
        android:id="@+id/btn_album"
        android:layout_width="230dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btn_camera"
        android:layout_centerHorizontal="true"
        android:text="@string/btn_main_gallery" />

    <Button
        android:id="@+id/btn_template"
        android:layout_width="230dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btn_album"
        android:layout_centerHorizontal="true"
        android:text="@string/btn_main_template" />

    <Button
        android:id="@+id/btn_exit"
        android:layout_width="230dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btn_template"
        android:layout_centerHorizontal="true"
        android:text="@string/btn_main_exit" />

</RelativeLayout>